[pytest]
asyncio_mode = auto
testpaths = tests
# Coverage is enforced on every run — raise the threshold as coverage grows.
addopts = --cov=services --cov=repositories --cov=cloudflare --cov=routes --cov-report=term-missing --cov-fail-under=75
